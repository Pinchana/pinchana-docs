---
title: Endpoint reference
description: Complete public gateway and administrator route reference.
---

import { Aside } from '@astrojs/starlight/components';

The canonical base URL is `https://api.pinchana.cc`. A self-hosted origin exposes the same gateway routes.

## Core routes

### `POST /v1/scrape`

Authenticates with `X-API-Key`, accepts a [`ScrapeRequest`](/api/schemas/#scrape-request), selects a module from the URL, and returns the normalized [API v1 success or error envelope](/api/schemas/). Container forwarding uses a 10-second connection timeout and a 120-second total timeout.

### `POST /scrape` (legacy)

Uses the same authentication, request body, routing, and forwarding limits but returns the earlier flat response. Existing clients may continue to use this route. New clients should use `/v1/scrape`.

### `GET /media/{platform}/{post_id}/{filename:path}`

Authenticates with `X-API-Key` and returns a file from the shared cache. `platform` is part of the stable URL shape but file lookup is based on `post_id` and `filename`. Absolute/traversal paths are rejected. FastAPI `FileResponse` provides normal metadata and byte-range behavior through the ASGI server.

### `GET /health`

Public aggregate health check. It calls every configured in-process and container module. A fully healthy gateway returns `200`:

```json
{"status":"healthy","modules":{"instagram":{"mode":"container","status":"healthy","detail":{"status":"healthy","service":"instagram","vpn":{"status":"running"}}}}}
```

If any module is unhealthy, the route returns `503` with the module map inside `detail`.

## Administrator routes

All administrator routes require `X-API-Key`.

| Method and path | Success response | Notes |
| --- | --- | --- |
| `POST /admin/vpn/rotate` | `{"status":"rotated"}` | Stops/starts the Gluetun tunnel through its control API and waits for a public IP. Rotation has a 90-second in-process cooldown. |
| `GET /admin/vpn/status` | `{"vpn": {...}, "public_ip":"..."}` | Returns Gluetun status and a nullable public IP. With `VPN_ENABLED=false`, status reports disabled and IP is `null`. |
| `GET /admin/modules` | In-process patterns and container configuration/running state | Available regardless of dynamic container mode. Source paths and internal endpoints may be sensitive; restrict operational access. |
| `POST /admin/modules/{name}/start` | `{"status":"started","endpoint":"..."}` | Requires `CONTAINER_MODE=true`; returns `501` otherwise and `404` for an unknown module. |
| `POST /admin/modules/{name}/stop` | `{"status":"stopped"}` | Requires `CONTAINER_MODE=true`. Stopping an already absent known module is idempotent. |

<Aside type="caution">
Compose already owns service lifecycle. Dynamic container mode mounts the Docker socket and can build/run modules; leave it disabled in the canonical Compose deployment unless that behavior is explicitly required.
</Aside>

## Browser DLP routes

These routes use the signed browser bearer session, not `X-API-Key`, and are available only when the internal protocol-v2 DLP service is enabled and healthy.

| Method and path | Purpose |
| --- | --- |
| `GET /web/capabilities` | Advertise DLP availability, protocol version, and fixed quality/codec/container choices |
| `POST /web/dlp/jobs` | Allocate a job and ephemeral worker public key |
| `POST /web/dlp/jobs/{jobId}/submit` | Submit one validated URL, fixed quality/codec/container choices, and optional encrypted cookies |
| `GET /web/dlp/jobs/{jobId}` | Poll queue, execution, progress, expiry, or failure state |
| `GET /web/dlp/jobs/{jobId}/file` | Stream the completed temporary file with `no-store` caching |

See [Private downloads (DLP)](/api/private-downloads-dlp/) for the protocol, request bodies, cryptography, ownership rules, and state transitions.

## Browser scrape routes

These routes use the signed browser bearer session rather than `X-API-Key`.

| Method and path | Purpose |
| --- | --- |
| `POST /v1/web/scrape` | Return the normalized API v1 envelope and rewrite protected assets under `/web/media/...` |
| `POST /web/scrape` | Return the legacy flat response for older Web clients |
| `GET /web/media/{platform}/{post_id}/{filename:path}` | Stream one protected cache file to the owning browser session |

The official Next.js application exposes same-origin `/api/scrape` and `/api/media/...` proxies. Browser code never receives the API machine key or private API origin.

## Public Web metadata

`GET /web/build` returns `{"version":"preview","commits":{...}}` without authentication. Official images bake the parent API revision and public submodule repository/revision pairs during CI. The gateway validates entry names, hexadecimal commits, and public HTTPS GitHub repository URLs before returning them; malformed or private-looking entries are omitted. The Web BFF sanitizes the response again before showing it in About diagnostics.

## OpenAPI discovery

FastAPI supplies:

- `GET /docs` — Swagger UI
- `GET /redoc` — ReDoc
- `GET /openapi.json` — the generated OpenAPI document

These reflect the gateway routes and response models mounted in that running instance, including any in-process plugin routers. Container modules expose their own OpenAPI documents on their internal service origins; the public gateway document does not require every module to be online.
